import { PageShell } from '../components/PageShell';

export function CollocationSystemPage() {
  return (
    <PageShell
      eyebrow="Collocation System"
      title="Map natural word pairings in a deliberate visual system."
      description="The base layout leaves room for collocation clusters, relationship views, and future semantic grouping without adding behavior yet."
      accent="radial-gradient(circle at top left, rgba(158, 146, 129, 0.32), transparent 45%), linear-gradient(180deg, rgba(255,255,255,0.58), rgba(236,229,221,0.96))"
    />
  );
}
